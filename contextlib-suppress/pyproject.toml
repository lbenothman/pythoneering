[project]
name = "contextlib-suppress"
version = "0.1.0"
description = "Clean exception handling with contextlib.suppress"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"