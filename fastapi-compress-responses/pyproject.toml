[project]
name = "fastapi-compress-responses"
version = "0.1.0"
description = "FastAPI GZip Middleware for compressing large responses"
requires-python = ">=3.9"
dependencies = [
    "fastapi>=0.115.6",
    "uvicorn>=0.34.0",
]