[project]
name = "fastapi-orjson"
version = "0.1.0"
description = "FastAPI with ORJson for faster JSON serialization"
requires-python = ">=3.9"
dependencies = [
    "fastapi>=0.115.6",
    "uvicorn>=0.34.0",
    "orjson>=3.10.14",
]