[project]
name = "fastapi-rate-limit"
version = "0.1.0"
description = "FastAPI rate limiting to protect against abuse"
requires-python = ">=3.9"
dependencies = [
    "fastapi>=0.115.6",
    "uvicorn>=0.34.0",
    "slowapi>=0.1.9",
]