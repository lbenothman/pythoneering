[project]
name = "fastapi-request-context"
version = "0.1.0"
description = "FastAPI request context using contextvars for async-safe request-scoped data"
requires-python = ">=3.9"
dependencies = [
    "fastapi>=0.115.6",
    "uvicorn>=0.34.0",
]