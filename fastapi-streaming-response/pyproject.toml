[project]
name = "fastapi-streaming-response"
version = "0.1.0"
description = "FastAPI Streaming Response for efficient data transfer"
requires-python = ">=3.9"
dependencies = [
    "fastapi>=0.115.6",
    "uvicorn>=0.34.0",
]