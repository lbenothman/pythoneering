[project]
name = "functools-partial"
version = "0.1.0"
description = "Pre-fill function arguments with functools.partial"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"