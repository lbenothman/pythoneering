[project]
name = "walrus-operator"
version = "0.1.0"
description = "Assignment expressions with the Walrus operator (:=)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"